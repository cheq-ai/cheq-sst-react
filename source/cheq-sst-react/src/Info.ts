// Replaced at build time by tsup with the version from package.json (see tsup.config.ts).
declare const __LIB_VERSION__: string;

export const LIBRARY_NAME = "cheq-sst-react";
export const LIBRARY_VERSION = __LIB_VERSION__;
export const UUID_KEY = "cheq.sst.http:uuid";